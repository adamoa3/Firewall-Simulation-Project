This application uses Python and the scapy library to read in packets from the host device's network interface. Each packet is processed and information is displayed on the GUI written with PyQt6. The user can use the interface to set their own firewall rules, and can toggle them on and off. Along with packet information, it is displayed whether each packet would be accepted or blocked depending on the active firewall rules.
